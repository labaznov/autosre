# Словарь в именах

Определения терминов — в [`SPEC.md` §3](SPEC.md). Здесь только отображение:
каким именем термин зовётся в коде, в базе и в интерфейсе, и какие слова для
него запрещены.

Правило простое: **если понятие есть в словаре, у него ровно одно имя в
каждом слое**. Синоним в коде — дефект, а не стилистика.

## Правила именования

- типы — единственное число, `PascalCase`: `Incident`, `Deviation`;
- таблицы — множественное, `snake_case`: `incidents`, `deviations`;
- поля — `snake_case`, без префикса имени таблицы: `weight`, а не `incident_weight`;
- имена латиницей, тексты для человека — по-русски;
- слово из колонки «не говорим» не встречается ни в коде, ни в интерфейсе,
  ни в разговоре.

## Наблюдение

| Термин | Тип | Таблица и поля | Не говорим |
| :--- | :--- | :--- | :--- |
| **Бакет** | `Bucket` | `buckets`: `stream`, `at`, `value` | наблюдение, замер, точка |
| **Ряд** | — (собирается из `buckets`; страница `/series`) | `buckets` одного `stream` подряд | история, таймсерия |
| **Поток** | `Stream` | `buckets.stream` | лог, лейбл, стрим |
| **Сервис** | `Service` | `incidents.service` | приложение, компонент |
| **Сигнатура** | `Signature` | `incidents.signature` | шаблон, паттерн, маска |
| **Горизонт** | `Horizon` | конфигурация: `width`, `period`; `incidents.horizon`, `deviations.horizon` | уровень, окно, срез |
| **Вид бакета** | `Kind` | `buckets.kind`: `Sum` — счётчик, `Mean` — уровень | тип |
| **Промежуток** | `Span` | — (минуты `from` включительно до `to` исключительно) | интервал, диапазон |
| **Час** | `Hour` | `hours` — свёрнутые бакеты | агрегат |
| **Коннектор** | — (крейты `autosre-logs`, `autosre-metrics`) | — | драйвер, адаптер |
| **Источник** | `Source` (трейт границы: так источник виден агенту) | `buckets.source` | — |

## Конвейер

| Термин | Тип | Таблица и поля | Не говорим |
| :--- | :--- | :--- | :--- |
| **Отклонение** | `Deviation` | `deviations`: `stream`, `horizon`, `at`, `score`, `weight`, `sifted`, `muted` | аномалия, алерт, срабатывание |
| **Отсев** | `Triage` (решение модели) | `deviations.sifted` — почему отсеяно; `incidents.because` — почему пропущено | фильтр, префильтр |
| **Подозрение** | — (отклонение, прошедшее отсев) | `deviations.incident IS NOT NULL` | кандидат, находка |
| **Вес** | — (`f64`) | `deviations.weight`, `incidents.weight` | приоритет, важность, ранг |
| **Расследование** | — (строки `investigations`; `Finding` — как его читают) | `investigations`, `steps` | анализ, разбор |
| **Досье** | — | `steps`: `tool`, `about`, `data` | контекст, дамп |
| **Вывод** | `Conclusion` | `investigations`: `cause`, `confidence`, `advice` | вердикт, заключение, разбор |
| **Важность** | `Severity` | `incidents.severity`: `low`, `medium`, `high` | вес, приоритет, критичность |

## Инцидент и работа с ним

| Термин | Тип | Таблица и поля | Не говорим |
| :--- | :--- | :--- | :--- |
| **Инцидент** | `Incident` | `incidents`: `service`, `signature`, `state`, `began`, `last`, `closed` | находка, кейс, тикет, проблема |
| **Счёт** | `Tally` | — (инциденты и оценки за промежуток) | статистика |
| **Оценка** | `Verdict` | `incidents.verdict`, `judge`, `judged` | фидбек, реакция, лайк |
| **Связь** | — | `links`: `incident`, `related` | линк, ссылка |
| **Приглушение** | `Mute` | `mutes`: `service`, `signature`, `until`, `author`, `reason` | игнор, снуз, подавление |
| **Заявка** | `Inquiry` | `inquiries`: `incident`, `command`, `reason`, `answer` | реквест, запрос, задание |
| **Приоритет** | — (не реализован; четвёртый срез) | конфигурация сервиса | вес, важность |

Состояния инцидента (`State`): `Open`, `Closed`, `Merged` — открыт, закрыт,
влит в другой. Состояния расследования и заявки — строки в базе без своего
типа; это долг ([`REVIEW.md`](REVIEW.md) §3.2).

Формы чтения в хранилище называются причастием от действия: `Finding` —
что расследование нашло, `Asked` — что спросили, `Written` — что написали,
`Muted` — что приглушили, `Filed` — что подшили, `Learned` — чему научились,
`Watched` — за чем смотрят, `Sifted` — что отсеяли. Это не термины, а формы:
термин у них тот же, что у таблицы.

## Живые данные

| Термин | Тип | Таблица и поля | Не говорим |
| :--- | :--- | :--- | :--- |
| **Урок** | `Lesson` | `lessons`: `kind`, `system`, `ask`, `answer` | сэмпл, пример, лог модели |
| **Оценка отсева** | `Verdict` | `deviations`: `verdict`, `judge`, `judged` | фидбек, разметка |
| **Корпус** | — (выгрузка `/api/corpus`) | — | датасет, дамп, обучающая выборка |

## Знания и отчёты

| Термин | Тип | Где лежит | Не говорим |
| :--- | :--- | :--- | :--- |
| **Скилл** | `Skill` | `skills/*.md` | правило, сценарий, плейбук |
| **Сигнал** | — (`when.signal` скилла) | `errors`, `metric`, `any` | тип, вид, источник |
| **Заметка** | `Note` | `notes/*.md` | статья, документ, вики-страница |
| **Черновик** | `Draft` | `drafts/*.md` | предложение, кандидат |
| **Отчёт** | `Report` | `reports/**/*.md` | сводка, дайджест |

## Связи

- **Бакет** принадлежит одному **потоку**; **ряд** — это бакеты одного потока
  подряд.
- **Отклонение** рождается из **ряда** на одном **горизонте**.
- **Отсев** превращает **отклонение** в **подозрение** или отбрасывает его.
- **Подозрение** попадает в очередь по своему **весу** и порождает
  **расследование**.
- **Расследование** состоит из шагов, собирает **досье** и заканчивается
  **выводом**.
- **Инцидент** собирает подозрения одной пары «**сервис** плюс **сигнатура**»,
  несёт **важность**, получает **оценку** дежурного и может иметь **связи**
  с другими инцидентами.
- **Заявка** принадлежит одному **инциденту** и ждёт ответа человека.

## Разговор, проверяющий термины

> **Разработчик:** Дежурный оценивает подозрение или инцидент?
>
> **Заказчик:** Инцидент. Подозрения он вообще не видит — они внутренние.
> Кнопка «по делу или ложное» стоит на карточке инцидента.
>
> **Разработчик:** Тогда «доля ложных подозрений» в приёмке названа неверно —
> считаться будет доля ложных **инцидентов**.
>
> **Заказчик:** Да, и это разные числа: одно подозрение может не дойти до
> инцидента, а один инцидент собирает десяток подозрений.
>
> **Разработчик:** А важность ставит модель в выводе или мы считаем её из веса?
>
> **Заказчик:** Модель. Вес — наш, он только про очередь: кого разбирать
> первым. Важность — для человека, она про то, насколько всё плохо.

## Найденные неоднозначности

**«Наблюдение» покрывало два разных понятия** — одно значение за минуту и
последовательность значений. Разведено: **бакет** и **ряд**. Слово
«наблюдение» выведено из терминов в обиходные, как «аномалия».

**Три разные «важности»** жили под похожими словами. Разведены:
**вес** — число агента для очереди; **важность** — категория от модели для
человека; **приоритет** — настройка сервиса в конфигурации. Употреблять их как
синонимы нельзя ни в коде, ни в разговоре.

**«Находка» означала то подозрение, то инцидент.** Слово запрещено. Дежурный
видит и оценивает **инцидент** — значит и метрика приёмки считается по
инцидентам, а не по подозрениям. Формулировка в `SPEC.md` §12 исправлена.

**«Сигнал» разошёлся с именем источника.** Источник зовётся `metrics` — это
имя коннектора, внутреннее дело агента. Сигнал в скилле зовётся `metric` —
единственное число, как всё в этом словаре: метрика одна, а ошибок много,
оттого `errors` рядом с ним во множественном. Отображение одного в другое
живёт в единственном месте, в `autosre_skills::signal`; до этого оно было
размазано и разъехалось — скилл `slow-degradation` не применялся ни разу.

**«Заявка» просилась в код как `Request`** и столкнулась бы с HTTP-запросом в
каждом слое. Выбран `Inquiry`. Таблица связей зовётся `links`: она старше
словаря, а переименование таблицы ломает откат на прежнюю версию
([ADR-0023](adr/0023-deploy-with-downtime.md)). Типа у связи нет — это пара
номеров.

**«Разбор» был синонимом расследования** и одновременно названием его
результата. Результат теперь **вывод** (`Conclusion`), а метрика приёмки зовётся
«время до вывода».

**«Источник» и «коннектор» смешивались.** Источник — внешняя система
(VictoriaLogs); коннектор — наш код, который в неё ходит. Разные вещи, оба слова
остаются.
